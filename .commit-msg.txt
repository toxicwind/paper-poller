paper-poller: PAPER-TASK/PAPER-RESULT poller daemon + watchdog + leg racer

- bin/race_papers.py: stdlib-only arXiv + alphaXiv racer (endpoint shapes
  borrowed from emergent-enrich route.py, public no-key); per-leg fail-fast
  timeouts, latency winners JSONL, HTTP-429 cooldown per leg (HFT rules)
- bin/poller.py: pitchfork daemon; polls fleet channel, atomic mkdir claim
  per task id, races legs, posts PAPER-RESULT with titles/IDs/URLs/relevance
- bin/watchdog.py: external watchdog; SIGKILLs wedged poller, escalates to
  pitchfork start (never --force); own /health endpoint
- seed-demo/: 2026-09-14 18:26 MDT seed race (race.py + 24-paper out.jsonl)
Lane 7, Chris 18:26 MDT order: paper research first-class.
